<%= error_messages_for :serviced_zip %>

<% form_for :serviced_zip, :url => serviced_zips_path(@serviced_zip) do |f| %>
	<%= render :partial => 'form', :locals => { :create => true, :f => f } %>
<% end %>
